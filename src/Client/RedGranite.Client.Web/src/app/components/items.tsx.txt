/*import React from "react";
import { createSelector } from "reselect";
import styled from "styled-components";
import { useAppSelector } from "../hooks";
import { makeSelectItems } from "../containers/HomePage/selectors";

const ItemsContainer = styled.div`
  max-width: 1280px;
  width: 100%;
  height: 100%;
  display: flex;
  justify-content: space-evenly;
  flex-wrap: wrap;
`;

const ItemContainer = styled.div`
  width: 17em;
  height: 18em;
  display: flex;
  flex-direction: column;
  align-items: center;
`;

const stateSelector = createSelector(makeSelectItems, (items) => ({
  items,
}));

export function Items() {
  const { items } = useAppSelector(stateSelector);

  const isEmptyItems =
    !items || items.length === 0;

  if (isEmptyItems) return <div>Loading...</div>;

  return (
    <ItemsContainer>
      {items && items.map((item) => (
        <ItemContainer>
          <h5>Name: {item?.name}</h5>
          <h5>Description: {item?.name}</h5>
          </ItemContainer>
      ))}
    </ItemsContainer>
  );
}
*/